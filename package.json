{
  "name": "pkext",
  "version": "0.1.0",
  "description": "Perkeep Browser Extension",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Angel",
  "dependencies": {
    "perkeep": "../perkeepjs/",
    "webextension-polyfill": "^0.3.1"
  }
}
